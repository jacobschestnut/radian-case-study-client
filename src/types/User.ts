export type User = {
    first_name: string;
    middle_initial: string;
    last_name: string;
    email: string;
    date_of_birth: string;
    address: string;
    tier: string;
    billing_period: string;
};